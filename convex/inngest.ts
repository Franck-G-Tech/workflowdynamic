// convex/inngest.ts
import { Inngest } from "inngest";

// Inicializamos el cliente. El ID puede ser cualquiera, ej: "mi-app-vacaciones"
export const inngest = new Inngest({ id: "gestion-vacaciones" });